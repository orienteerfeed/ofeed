{{- if .Values.board.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ofeed.boardServiceName" . }}
  labels:
    {{- include "ofeed.labels" . | nindent 4 }}
    app.kubernetes.io/component: board
spec:
  replicas: {{ .Values.board.replicaCount }}
  selector:
    matchLabels:
      {{- include "ofeed.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: board
  template:
    metadata:
      labels:
        {{- include "ofeed.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: board
      {{- with .Values.board.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 101
        runAsGroup: 101
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: board
          image: "{{ .Values.board.image.repository }}:{{ required "board.image.tag is required" .Values.board.image.tag }}"
          imagePullPolicy: {{ .Values.board.image.pullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          ports:
            - name: http
              containerPort: {{ .Values.board.containerPort }}
              protocol: TCP
          env:
            - name: PORT
              value: {{ .Values.board.containerPort | quote }}
            - name: BOARD_API_UPSTREAM
              value: http://{{ include "ofeed.apiServiceName" . }}:{{ .Values.api.service.port }}
            {{- range $key, $value := .Values.board.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
          resources:
            {{- toYaml .Values.board.resources | nindent 12 }}
      {{- with .Values.board.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.board.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.board.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
